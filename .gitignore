# ── 대화 데이터·인덱스 (개인정보 — 절대 커밋 금지) ──
data/
*.db
*.db-wal
*.db-shm
*.npy
*.npy.tmp
vector_ids.json
vector_ids.json.tmp
*.log

# ── 설정 파일(API 키 포함 가능 — 절대 커밋 금지). 예시(config.env.example)만 커밋 ──
config.env
*.env
!*.env.example

# ── 파이썬 ──
__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/
build/
dist/
node_modules/
.venv/
venv/
env/

# ── 임베딩 모델 캐시 ──
fastembed_cache/
*.onnx

# ── OS/에디터 ──
.DS_Store
Thumbs.db
.vscode/
.idea/

# PyInstaller
*.spec
packaging/*.log

# editor/backup junk
*.bak
restore_test_state.txt
