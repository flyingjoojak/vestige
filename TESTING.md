# 릴리스 전 수동 테스트 체크리스트

v0.2.1 → 다음 릴리스(0.3.0 후보) 사이에 들어간 변경을 손으로 확인하는 목록.
자동 테스트(pytest 334개 + tsc/lint/build)가 못 잡는 것 — **실제 클릭 동선, 데이터가 쌓인 상태에서의 동작, 설치본에서만 드러나는 문제** — 에 집중한다.

> **프론트를 다시 빌드했으면 브라우저를 강제 새로고침(Ctrl+Shift+R)하고 시작할 것.**
> 3D 지도는 별도 청크로 lazy-load돼서, 빌드하면 청크 해시가 바뀌어 열려 있던 페이지가
> 사라진 파일을 import하려다 실패한다(지도만 먹통으로 보인다).

> 설치본 버전 표기는 아직 `0.2.1`이다(버전 범프는 릴리스 때). 오늘 작업분이 들어갔는지는
> **설정 > 일반 > 로그 폴더에 '원본 로그 보존소' 행이 있는지**로 구분하면 확실하다.

---

## 0. 설치·기동 (설치본에서만 확인 가능)

- [ ] 설치 프로그램이 정상 실행되고 설치가 끝난다
- [ ] 앱이 뜨고 좌측 메뉴에 **검색 / 세션 / 군집 / 지도 / 폴더 / 접힘 / 설정** 7개가 보인다
- [ ] 하단 상태바에 세션·대화·벡터 수가 뜨고, 숫자가 **1초 간격**으로 갱신된다
- [ ] 앱을 껐다 켜도 기존 아카이브(대화 수)가 그대로다
- [ ] (중요) 기존 0.2.1 DB를 그대로 열었을 때 오류 없이 뜬다 — **스키마 마이그레이션 0005·0006이 도는 지점**

## 1. 접기 (#128)

### 세션 화면에서
- [ ] 대화의 턴에 마우스를 올리면 오른쪽에 **접기 아이콘**이 뜬다
- [ ] 접으면 그 자리에 **점선 한 줄**(`#번호 · 접힘 · 요약 · 펼치기`)로 바뀐다 — 사라지지 않아야 한다
- [ ] `펼치기`를 누르면 원래 대화로 돌아온다
- [ ] 헤더에 `접힘 N개`가 표시된다
- [ ] `세션 전체 접기` → **앱 내 모달**로 확인을 묻는다(브라우저 기본 팝업이 아니어야 함)
- [ ] 전체를 접으면 헤더 버튼이 `전체 펼치기`로 바뀌고, 누르면 모두 복구된다

### 검색에서
- [ ] 검색 결과에서 접으면 그 줄이 **`접힘` 한 줄로 남는다**(즉시 사라지면 안 됨)
- [ ] 같은 검색어로 **다시 검색하면** 그 결과가 빠진다
- [ ] 접힌 줄의 `펼치기`로 되돌릴 수 있다

### 목록·지도·내보내기
- [ ] 세션 전체를 접으면 세션 목록에서 **흐린 `접힘` 뱃지**로 남는다(사라지면 안 됨)
- [ ] 접은 뒤 **지도 탭**을 열면 그 대화가 빠져 있다(지도 캐시 무효화 확인)
- [ ] 펼치면 지도에 다시 나타난다
- [ ] 접힌 턴은 세션 markdown 내보내기에서도 빠진다

### 접힘 모아보기
- [ ] 좌측 `접힘` 탭에 접은 것들이 **최근 순**으로 모인다
- [ ] 메뉴 아이콘에 **개수 배지**가 뜨고, 다른 화면에서 접어도 몇 초 내 반영된다
- [ ] `세션 열기`를 누르면 그 세션이 열리고 **해당 턴으로 스크롤·강조**된다 ← 최근 고친 부분
- [ ] 세션 중간쯤(30번째 근처) 턴으로 들어가도 위치가 정확하다 ← 렌더 창 타이밍 버그 수정분
- [ ] `펼치기`를 누르면 목록에서 빠지고 배지 숫자가 줄어든다

## 2. 폴더 (#201)

### 만들기·구조
- [ ] `폴더` 탭에서 `+`로 새 폴더를 만든다 — **앱 내 모달**로 이름을 묻는다
- [ ] 폴더를 고르고 `하위 폴더`로 중첩을 만든다
- [ ] 트리에서 화살표로 펼침/접힘이 된다
- [ ] `이름 변경` / `삭제`가 모달로 뜨고 정상 동작한다
- [ ] 삭제 시 **하위 폴더까지** 지워지고, **대화 원문은 남아있다**(세션 탭에서 확인)

### 담기
- [ ] 검색 결과에서 폴더+ 아이콘 → 폴더 선택 → 담긴다(체크 표시로 알림)
- [ ] 같은 항목을 **두 번 담아도** 중복되지 않는다
- [ ] 같은 항목을 **여러 폴더에** 담을 수 있다
- [ ] 대화의 개별 턴에서도 담긴다
- [ ] 세션 헤더의 `폴더에 추가`로 **세션 통째**가 담긴다
- [ ] 드롭다운의 `새 폴더 만들어 담기`가 동작한다

### 보기·검색
- [ ] 항목을 누르면 **폴더 화면 안 오른쪽에 대화가 열린다**(세션 탭으로 튕기면 안 됨) ← 최근 변경
- [ ] 폴더 검색창에 입력하면 **그 폴더 범위로만** 결과가 나온다
- [ ] 상위 폴더에서 검색하면 **하위 폴더 내용까지** 포함된다
- [ ] 빈 폴더에서 검색하면 결과 0건이 뜬다(에러가 아니라)
- [ ] 검색창을 비우면 다시 폴더 내용 목록으로 돌아온다
- [ ] 검색 결과를 눌러도 폴더 화면 안에서 열린다

### 순서·별칭
- [ ] 항목에 마우스를 올리면 왼쪽에 **위/아래 버튼**이 뜬다
- [ ] 순서를 바꾸고 **다른 폴더 갔다 돌아와도** 순서가 유지된다
- [ ] 연필 아이콘으로 **별칭**을 넣으면 그 폴더에서만 제목이 바뀐다
- [ ] **원본 제목은 그대로다** — 세션 탭/검색에서 원래 제목으로 나오는지 확인
- [ ] 같은 대화를 다른 폴더에 담고 **다른 별칭**을 붙일 수 있다
- [ ] 별칭이 붙은 항목에 태그 아이콘이 보인다

### 드래그 이동·정렬
- [ ] 폴더 안 **항목**을 드래그하면 놓일 자리에 **회색 삽입선**이 뜬다
- [ ] 놓으면 그 자리에 들어가고, 새로고침해도 순서가 유지된다
- [ ] 트리에서 **폴더**를 드래그하면 삽입선 + 들어갈 폴더에 테두리가 뜬다
- [ ] 폴더를 다른 폴더 **안으로** 넣을 수 있다(가운데 영역에 놓기)
- [ ] 폴더를 형제 **사이로** 옮길 수 있다(위/아래 가장자리에 놓기)
- [ ] 목록 아래 빈 영역에 놓으면 **최상위로 빠진다**
- [ ] 자기 자신이나 **자기 하위 폴더로는 못 옮긴다**(금지 표시, 놓아도 안 바뀜)
- [ ] 드래그 중 화면이 덜컥거리거나 삽입선이 깜빡이지 않는다
- [ ] 드래그를 취소(Esc·바깥에 놓기)해도 원래 자리에 남는다

### 세션 참조의 동적 확장 (설계 핵심)
- [ ] 세션을 폴더에 담아둔 뒤 **그 세션으로 대화를 더 진행**하고 색인한다
- [ ] 폴더의 `대화 N개 범위` 숫자가 **늘어난다**(스냅샷이 아니라 참조이므로)

## 2-1. 목록 검색창 · 세션 제목 (#210)

- [ ] `세션` 탭 상단 검색창에 **세션 제목 일부**를 넣으면 목록이 걸러진다
- [ ] **세션 ID 일부**(앞 8자리 등)로도 걸러진다
- [ ] `군집` 탭 상단 검색창은 **군집 이름**으로 걸러진다
- [ ] 검색창을 비우면 전체 목록으로 돌아온다
- [ ] 세션을 고르고 **제목 변경**을 누르면 모달이 뜨고, 바꾼 제목이 목록에 반영된다
- [ ] 바꾼 제목이 **폴더 화면에서도** 같게 보인다 ← 오늘 고친 부분
- [ ] 제목을 **비워서 저장**하면 기본 제목(첫 턴 요약)으로 돌아온다 ← 오늘 고친 부분
- [ ] 세션 목록 행에서 `폴더에 담기` 드롭다운이 **잘리지 않고** 전부 보인다
- [ ] 폴더가 많을 때 드롭다운 **목록을 스크롤해도 닫히지 않는다** ← 오늘 고친 부분

## 3. 세션 복구 (#163)

- [ ] 설정 > 일반 > 로그 폴더에 **원본 로그 보존소** 행이 있고 현재 용량이 보인다
- [ ] 보존소 `경로 변경`으로 다른 폴더를 지정하면 저장된다
- [ ] 경로를 바꿔도 **기존 보존 파일은 그대로 남아있다**(도움말대로 자동 이동 안 함)
- [ ] 용량 상한(MB)을 넣고 저장하면 유지된다(빈 값 = 무제한)
- [ ] 원문이 사라진 세션에 **복구** 버튼이 보이고, 눌러 복구 후 `열기`가 활성화된다
- [ ] 원문도 보존본도 없는 레거시 세션에 **markdown으로 내보내기** 링크가 보이고 파일이 받아진다

### 보존소 안전장치 (오늘 추가 - 데이터 손실 방지)
- [ ] 보존소 폴더에 `.vestige-raw` 파일이 생겨 있다(숨김 파일 보기 필요)
- [ ] **기존 파일이 있는 폴더**(예: 다운로드 폴더)를 보존소로 지정하면 마커가 **안 생긴다**
- [ ] 그 상태에서 용량 상한을 켜고 색인해도 **그 폴더의 파일이 하나도 안 지워진다**
  (색인 로그에 `ERROR raw archive quota 정리 실패`가 뜨는 게 정상 - 안전하게 거부한 것)
- [ ] 보존소 경로를 되돌리면 정상 동작으로 돌아온다
- [ ] 진행 중인 세션을 색인한 뒤 **복구해보면 같은 줄이 두 번 들어가 있지 않다** ← 커서 버그 수정분

## 4. 설정 화면 정합성

- [ ] 로그 폴더 섹션의 **네 행(Claude Code / Codex / 원본 보존소 / Claude CLI)** 이 모두 같은 모양이다
- [ ] 네 행 모두 **실제 경로가 펼치지 않아도 보인다**
- [ ] `색인 소스` 섹션에는 경로가 **중복 표시되지 않는다**(켜기/끄기만)
- [ ] 경로 앞뒤에 공백을 넣고 저장하면 **입력칸도 공백이 정리된 값**으로 바뀐다
- [ ] 설정 화면 폭이 넓어져 긴 경로가 덜 잘린다

## 5. 모달 (전역)

브라우저 기본 팝업(창 최상단 OS 스타일)이 **하나도 뜨지 않아야** 한다.

- [ ] 폴더 만들기 / 하위 폴더 / 이름 변경 / 삭제
- [ ] 폴더 항목 별칭 변경
- [ ] 새 폴더 만들어 담기
- [ ] 세션 전체 접기
- [ ] 사용 중인 세션 강제 재개(세션이 최근 수정된 경우에만 뜸)
- [ ] 입력 모달이 **자동 포커스 + 기존 값 전체 선택** 상태로 열린다
- [ ] **Enter**로 확인, **Esc**·바깥 클릭으로 취소된다(취소해도 화면이 멈추지 않음)
- [ ] **폴더 이름**은 빈 값이면 확인 버튼이 비활성이다
- [ ] **세션 제목·폴더 별칭**은 빈 값으로도 저장된다(= 기본값으로 되돌리기) ← 오늘 고친 부분

## 6. 회귀 확인 (기존 기능이 안 깨졌는지)

- [ ] 검색(통합/의미/키워드)이 정상 동작하고 결과를 클릭하면 대화가 열린다
- [ ] 날짜 필터·출처 필터가 동작한다
- [ ] 세션 목록·군집 목록이 정상이다
- [ ] 3D 지도가 뜨고 점을 클릭하면 그 대화로 이동한다
- [ ] 세션 `열기`(터미널 재개)가 동작한다
- [ ] 색인 `지금 색인`이 돌고 상태바에 진행률이 뜬다
- [ ] 요약(정제 AI) 설정 화면이 정상이고 `Claude CLI` 경로가 인식된다
- [ ] MCP 연동 탭이 정상이다
- [ ] 다크/라이트 테마, 한국어/English 전환이 정상이다

## 7. 데이터 안전 (릴리스 전 필수)

- [ ] **기존 0.2.1 DB로 실행** → 마이그레이션 후 대화 수가 그대로다
- [ ] 접기/폴더는 **원문을 지우지 않는다** — 접고 담고 지운 뒤에도 세션 탭에서 대화가 그대로 보인다
- [ ] 전체 재색인을 돌려도 **접힘 상태와 폴더 구성이 유지된다** ← 별도 테이블로 둔 이유
- [ ] 앱 종료 후 재실행 시 폴더·접힘·별칭·순서가 모두 남아있다

## 8. 풀리뷰 수정분 집중 확인 (#211)

릴리스 직전 7개 리뷰어가 잡은 차단 항목들. **여기만 따로 돌려도 가장 값이 높다.**

### 성능 (체감으로 확인)
- [ ] 하단 상태바 숫자가 1초마다 갱신되는데 **팬이 돌거나 앱이 무거워지지 않는다**
      (수정 전엔 매초 벡터 행렬 전체를 메모리에 올렸다)
- [ ] 턴을 **접은 직후 지도 탭**을 열면 바로 뜬다(수십 초 멈추면 안 됨)
- [ ] 접기 전후로 **군집 색깔이 유지된다**(전부 바뀌면 안 됨)
- [ ] 턴이 많은 세션에서 `전체 펼치기`가 한 번에 끝난다(하나씩 풀리면 안 됨)

### 실패가 보이는지
- [ ] 임베딩 모델을 끈 상태로 **폴더 안 검색** → "결과 없음"이 아니라 **에러 문구**가 뜬다
- [ ] 같은 상태로 세션/군집 탭 검색 → 역시 에러 문구가 뜬다
- [ ] 설정에서 보존소 경로를 **쓸 수 없는 경로**로 바꾸고 색인 → 색인 로그에 `ERROR`로 뜬다
      (수정 전엔 조용히 실패해서 30일 뒤에야 알 수 있었다)

### 언어
- [ ] **English로 전환**한 뒤 폴더를 자기 하위로 드래그 → 에러 문구가 **영어**로 뜬다
      (수정 전엔 한국어가 그대로 노출됐다)
- [ ] 빈 이름으로 폴더 만들기, 없는 폴더 조작 등도 영어로 뜬다

## 9. 릴리스 배포 검증 (태그 push 후)

기능이 아니라 **배포 경로**를 확인하는 단계. 여기까지 안 보면 "빌드는 green인데 사용자에게는
안 닿는" 상태를 놓친다(v0.3.0에서 tap 버전을 빼먹어 mac 사용자가 계속 0.2.1을 받았다).

- [ ] 릴리스에 **OS별 설치본 3개**가 첨부됐다 (`.exe` / `.dmg` / `.AppImage`)
      — mac 잡은 `continue-on-error`라 조용히 실패해도 릴리스는 green으로 만들어진다
- [ ] 자동 업데이트 매니페스트 3개가 있다 (`latest.yml` / `latest-mac.yml` / `latest-linux.yml`)
- [ ] **tap 저장소**([homebrew-vestige](https://github.com/flyingjoojak/homebrew-vestige))의
      `Casks/vestige.rb` `version`이 새 버전이다 ← **가장 자주 빠뜨리는 곳**
- [ ] cask가 만드는 dmg URL이 HTTP 200이다 (404면 버전 불일치 또는 dmg 누락)
- [ ] 릴리스 본문에 `<!--lang:ko-->` / `<!--lang:en-->` 마커가 있다(앱 업데이트 배너용)
- [ ] 다운로드한 설치본의 크기·sha512가 `latest.yml`에 적힌 값과 같다
- [ ] 이전 버전이 깔린 기기에서 **업데이트 배너가 새 버전을 감지**한다(Windows)
- [ ] `brew upgrade --cask vestige`로 mac이 새 버전을 받는다

> README의 '릴리스 & 버저닝' 섹션에 버전 문자열 네 곳의 역할표와 확인용 명령이 있다.

---

## 알려진 제약 (버그 아님)

- macOS 첫 실행 시 '확인되지 않은 개발자' 경고 — Apple 공증 미도입(#191)
- 폴더 트리 이동은 **드래그로만** 가능(키보드·클릭 대체 수단 없음 — WCAG 2.5.7, 후속 과제)
- 접힌 턴이 검색 결과의 **스레드 문맥**에는 여전히 나올 수 있음(히트 자체는 제외됨)
- 폴더 항목이 많으면 폴더 화면 로딩이 항목 수에 비례해 느려짐(`folder_items()` N+1, 후속 과제)
- 보존소 gz는 append 중 강제 종료되면 손상될 수 있음(원자성 미적용, 후속 과제)
